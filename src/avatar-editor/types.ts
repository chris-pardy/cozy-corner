// Re-export all types from the shared module for backwards compatibility.
export {
    type SerializedPixelBuffer,
    type SerializedLayer,
    type AvatarTarget,
    type StatePropertyType,
    STATE_PROPERTY_TYPES,
    type StateProperty,
    type Behavior,
    ANIMATION_ACTIONS,
    DIRECTIONS,
    WELL_KNOWN_TARGETS,
} from '../shared-editor/types';
