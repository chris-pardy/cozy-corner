services:
  pds:
    image: ghcr.io/bluesky-social/pds:latest
    ports:
      - "2583:2583"
    volumes:
      - pds-data:/pds
    env_file:
      - pds.env
    restart: unless-stopped

volumes:
  pds-data:
