services:
  pds:
    image: ghcr.io/bluesky-social/pds:0.4
    ports:
      - "2583:3000"
    volumes:
      - pds-data:/pds
    env_file:
      - pds.env
    restart: unless-stopped

volumes:
  pds-data:
